// import {render, useState} from '../libs/customUI';
// import {Background, BackgroundProps} from './Background';
// import userEvent from '@testing-library/user-event';

// describe('[COMPONENT] Background test', () => {
//   test('click background test', async () => {
//     // given
//     const App = document.createElement('div');
//     const [isShow, setIsShow] = useState(true);

//     const testProps: BackgroundProps = {isShow: isShow, setIsShow: setIsShow};
//     render(App, Background, testProps);

//     const backgroundElement = App.querySelector('.background')!;
//     //when
//     await userEvent.click(backgroundElement);

//     //then
//     expect(backgroundElement.classList.contains('hidden')).toBeTruthy();
//   });
// });

export {};
